{
  "name": "whatsapp-wrapped",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WhatsApp Wrapped — 100% client-side chat analytics",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint js tests"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "vitest": "^2.1.8"
  }
}
